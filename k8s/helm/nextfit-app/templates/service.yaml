apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextfit-app.fullname" . }}
  labels:
    {{- include "nextfit-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nextfit-app.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP